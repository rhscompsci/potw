#include <bits/stdc++.h>

using namespace std;

static auto solve() {
    int n, c;
    cin >> n >> c;
    vector <pair<int, int>> a{};

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    short t;
    cin >> t;
    while (t--)
        solve();
}